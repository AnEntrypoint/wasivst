PIVOT (user-directed 2026-05-29): wasivst should drop v86 and adopt webix (AnEntrypoint/webix, c:/dev/webix) as the 64-bit execution engine. webix = a ~280-line host over jart/blink compiled to wasm (240KB blinkenlib.wasm). It runs REAL x86-64 Linux userspace: ELF64, musl-static AND alpine dynamic /bin/busybox + /sbin/apk via ld-musl, SSE2, fork/clone, ~150 syscalls. Library API: createBlinkHost({}), host.mountTarBytes(rootfsTar), host.runElf(bytes,{argv}). Byte-exact snapshot/restore of wasm memory+registers (=> warm-boot snapshot solves cold-boot latency). Browser host shim + witness page window.__debug.x86_64. CAVEATS (current build = POSIX NOJIT NOSOCK single-threaded): NOJIT=interpreter (slow, ~10x; realtime VST audio at-risk, offline render likely), no AVX (SIGILL), no TCP, no pthreads. To run VSTs: mount an Alpine+Wine rootfs tar, runElf wine with the Win64 VST host. Audio I/O via blink hostfs/MEMFS or stdin/stdout, NOT v86 serial.