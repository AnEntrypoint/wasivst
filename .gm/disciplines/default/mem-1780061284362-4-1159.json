webix v0.8.0 portabox build (blinkenlib.wasm sha 3b9351bb7d1c, witnessed AGENTS.md:65-94, test.js 19/19) ALREADY HAS: (1) THREADS ON - built -pthread --shared-memory --import-memory PTHREAD_POOL_SIZE=8; read live buffer from Module.HEAPU8.buffer (NOT wasmExports.memory); needs COOP/COEP crossOriginIsolated for SAB. (2) SOCKETS ON. (3) ZERO-COPY FRAMEBUFFER - guest publishes RGBA via synthetic syscall 0x5fb, host reads via blinkenlib_get_fb_* + blinkenlib_spy_address (host pointer into guest memory). (4) argv NUL-separated. (5) pipe() works but pipelines need fork (emscripten has NO fork - HAVE_FORK off, permanent). (6) JIT IMPOSSIBLE in wasm - NOJIT permanent. (7) FS persists across runElf within a page session (singleton host); cross-reload needs IDBFS wiring (not done). (8) streaming compile + Cache API (webix-wasm-v1). IMPLICATION FOR wasivst: blinkenlib_spy_address (zero-copy host<->guest memory window) is the audio transport primitive - NOT v86 serial, NOT a separate SAB ring we build. We register a guest audio buffer via a synthetic syscall and spy it from the host, exactly like the framebuffer. Threads enable a dedicated audio thread.