wasivst rootfs path WITNESSED feasible: Alpine v3.19 community repo ships wine for x86_64 (pkgs.alpinelinux.org). webix createApk already extracts real .apk into its rootfs (test.js). So assemble Alpine+Wine64 musl rootfs via apk add wine, tar it, host.mountTarBytes. The OPEN gate is whether wine RUNS under blink NOJIT/no-fork (mutable wine-runs-under-blink) - that needs a runtime spike (build rootfs, runElf wine64 trivial PE, capture syscall gaps like the observed __syscall_setsockopt).