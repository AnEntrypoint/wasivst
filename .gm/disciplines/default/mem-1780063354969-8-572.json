WITNESSED (CI run 26641633364, 2026-05-29): Alpine 3.19 + Wine64 rootfs assembles CLEANLY via apk-in-container. apk installed wine 8.21-r0 (398 MiB, 87 packages incl. libpulse, sane, libxkbcommon, freetype-chain); /usr/bin/wine64 -> wine symlink present, wineserver present. The ONLY failure was tar running as host user couldnt read root-owned /etc/shadow,/root - fixed by tarring inside the container. So the rootfs INPUT to the wine-under-blink spike is proven buildable on CI. Next run executes wine64 --version under blink to get the actual VERDICT + syscall gap map.